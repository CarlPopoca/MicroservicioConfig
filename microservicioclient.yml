server:  
  port: ${PORT:2222}

eureka:  
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

structure:  
  contact:
    name: microservicio-service
    findAllUrl: http://${structure.contact.name}/contact/findall/
	saveUrl: http://${structure.contact.name}/contact/save/
	deleteUrl: http://${structure.contact.name}/contact/delete/